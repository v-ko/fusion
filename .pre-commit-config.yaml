repos:
  - repo: local
    hooks:
      - id: tox
        name: tox (tests + format)
        language: system
        entry: tox --conf py-src/ --workdir py-src/build/tox
        pass_filenames: false
